return {
  "github/copilot.vim",
  event = "InsertEnter", -- Lazy load when entering insert mode to improve startup time
  config = function()
    -- Optional: Add custom configurations for Copilot
    -- For basic Copilot functionality, this config block can remain empty.
    -- Advanced settings like key remaps or specific behavior adjustments can go here.
  end,
}

